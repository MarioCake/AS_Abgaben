using School;
using System;

namespace Arrays
{
    class Program
    {
        static void Main(string[] args)
        {
            SchoolTask.LoadAssembly(typeof(Program).Assembly, args);
        }
    }
}
